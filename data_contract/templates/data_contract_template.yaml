# Унифицированный шаблон Data Contract (data_contract_template.yaml)

# 1. Описание процесса
# Служит для общего описания витрины, бизнес-смысла и высокоуровневых целей процесса загрузки данных.
description: ""

# 2. Режимы запуска
# Определяет, как и когда запускается процесс обновления витрины (вручную, по расписанию, по событию).
launch_modes:
  manual: false # Возможен ли запуск вручную
  schedule: ""  # Расписание (например, "каждый день в 9:00 на Т-1")

# 3. Входные и выходные данные
# Описывает параметры скрипта, а также целевые таблицы и ключи партицирования.
io_data:
  input_parameters: [] # Входные параметры для скрипта
  output_table: ""     # Название целевой таблицы витрины
  partitions: []       # Поля, по которым происходит партицирование

# 4. Используемые таблицы подписки / Источники
# Перечень всех таблиц-источников, справочников и реплик, необходимых для построения витрины.
source_systems:
  - table_name: "" # Имя таблицы/реплики
    description: "" # Описание источника
    attributes: []  # Ключевые атрибуты

# 5. Шаги процесса
# Высокоуровневое логическое описание основных этапов загрузки и трансформации.
process_steps:
  - step: 1
    description: "Подготовительные шаги"
  - step: 2
    description: "Отбор данных"
  - step: 3
    description: "Запись в таблицу витрины"

# 6. Подготовительные шаги
# Шаги для инициализации: получение путей, констант, фильтров и формирование вспомогательных датасетов.
preparatory_steps: []

# 7. Отбор данных
# Детальное описание алгоритмов фильтрации и объединения (JOIN) данных из таблиц-источников.
data_selection:
  - step: ""
    logic: "" # Описание JOIN'ов, WHERE-условий и трансформаций

# 8. Заполнение полей датасета (Маппинг)
# Маппинг атрибутов источника на поля целевой витрины с указанием формул и алгоритмов заполнения.
field_mapping:
  - target_field: "" # Название поля витрины
    type: ""         # Тип данных
    description: ""  # Описание поля
    algorithm: ""    # Алгоритм заполнения или источник

# 9. Запись данных в таблицу
# Алгоритм сохранения данных в целевую таблицу (метод полной замены, инкремент, удаление партиций).
data_loading:
  method: "" # Метод загрузки
  logic: ""  # Алгоритм (например, удалить старые партиции, вставить новые)

# 10. Требования к нефункциональным характеристикам / Критерии приемки
# Требования к производительности, объему данных, логированию, а также критерии качества прототипа.
non_functional_requirements:
  logging: ""
  frequency: ""
  volume: ""
  first_load: ""
  acceptance_criteria: []

# 11. Заметки
# Дополнительная информация, черновики SQL-запросов, ссылки на документацию (JIRA, Confluence).
notes: []
